1 thread创建线程
2 创建后主线程,子线程各自运行.当主线程结束而子线程位结束时,会报错
3 为防止报错,两种方法:
1> thread.join() 主线程等子线程运行完再执行
2> thread.datch() 分离,主线程子线程各自运行,如果主线程结束了子线程还未运行完,虽未起作用,但会在后台运行完,不会报错
4 在调用join或datch前,需要调joinable()确定能否调用
