Linux内核初步认识
1.操作系统(OS)是什么?

简单讲,是一个交互系统
可以理解为:
电脑 / 手机的大管家
硬件和软件之间的翻译官
你和设备之间的交互界面

它主要做什么？
管理硬件       控制 CPU、内存、硬盘、屏幕、键盘、摄像头、网络等，让它们协调工作。
运行软件       让微信、浏览器、游戏等应用能打开、使用，不会互相冲突。
提供操作界面    比如桌面、图标、窗口、文件管理器，让你不用写代码就能用电脑。
安全与权限管理  账号、密码、文件权限，防止程序乱改系统。

内核,是操作系统的核心部分
操作系统 =内核 + 图形界面 / 命令行 + 驱动 + 系统工具 + 库 + 桌面环境等
简单理解:
内核 = 发动机 ->在后台默默干活
操作系统 = 整辆汽车

2.内核是什么?
从功能层面,内核是一个中间层,实现软件和硬件的交互

软件 (打开bilibili)
|  软件与内核交互,告诉需求
V
内核
|  内核操作底层的硬件 比如:发送数据,往磁盘写东西
V
硬件 (真实存在的,储存数据)

从其他层面:
1)内核提供 API：->函数库
内核把自己能干的事，包装成一个个 “函数接口”，开放给外面的程序调用
客人(程序)看菜单（API）点菜，后厨（内核）帮你做好.这样,客人就不用进后厨了(程序不用知道硬件在干什么)
比如,调用的read函数,其实就是内核提供的一个api
内核开放了很多的接口(或者说,函数,或,喜用调用)给软件
2)内核是manager,给每个进程分配资源,以及决定了进程间共享多少资源,进程如何切换等

3.内核实现的策略:
宏内核:->全能型大公司
内核的所有代码,包括子系统(如内存管理,文件管理,设备驱动程序)都打包到一个文件.
内核的每个函数都可以访问到内核其他所有部分.
linux内核基于此实现
微内核:->极简型总部 + 外包公司
最基本的功能由中央内核(微内核),其他所有功能委托给一些独立进程

4.内核包含哪些核心的模块?
<1>进程的调度和切换
准备知识:
-什么是进程?
进程 = 正在运行的程序
eg:
你桌面上的 QQ、浏览器、游戏 这些文件，叫程序（静止的代码）
当你双击打开它，它跑起来了，就变成了进程
一句话：程序是死的，进程是活的。一个程序可以有n个进程

和前面学的内容串起来:
内核管理进程
内核给进程分配内存、分配 CPU 时间片
进程想操作硬件，必须通过内核 API（系统调用）
-什么是cpu?
CPU = 中央处理器,不停地取指令、算数据、做判断

和前面学的内容串起来:
内核负责管理、分配 CPU(一般一台电脑有4/8个)
进程要运行，就必须占用 CPU 时间
内核用调度策略决定：哪个进程先用 CPU、用多久->每个进程优先级不一样

比喻:
电脑 = 一家公司
CPU = 干活的员工
内核 = 经理（分配任务）
进程 = 一个个要做的任务


-进程的调度:
现在过来了100个任务(进程->比如,要打开100个app),经理(内核)决定给
每个员工(cpu)分多少任务(进程要运行，就必须占用 CPU 时间).
这个过程就是 调度
内核用调度策略决定：哪个进程先用 CPU、用多久

-进程的切换
现在我要从1进程换到处理2进程,这就是进程的切换
而内核需要考虑,如何优先调度级别高的进程

<2>内存的管理:
内核决定了多个进程的每一个分配多少资源

<3>虚拟内存机制:
虚拟内存 = 让程序以为自己独占一大块连续内存             -> 假的
实际物理内存是零散、共享、甚至用硬盘顶替的一套内核机制。   -> 实际
类似于:老板会让每个员工以为自己是最厉害的

和前面学的内容串起来:
内核管理虚拟内存、页表、缺页中断
进程只看到虚拟地址，完全不用管物理内存
CPU 有 MMU（内存管理单元）硬件加速地址翻译

<4>和网络交互的地方:


